{
  "name": "campus-anonymous-feedback-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client && npm run sync:client-dist && npm run build --workspace server",
    "sync:client-dist": "node scripts/sync-client-dist.mjs",
    "start": "npm run start --workspace server",
    "check": "npm run check --workspace server && npm run check --workspace client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "latest"
  }
}